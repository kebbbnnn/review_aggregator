{
  "name": "review-aggregator-api",
  "version": "1.0.0",
  "description": "Cloudflare Worker serving movie review aggregations from D1",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240101.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.109.2"
  }
}
